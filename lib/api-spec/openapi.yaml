openapi: 3.1.0
info:
  title: Api
  version: 0.1.0
  description: AL-RAHMAN TRADERS Ledger API
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: auth
    description: Authentication
  - name: customers
    description: Customer management
  - name: products
    description: Product/inventory management
  - name: saleOrders
    description: Sale orders & invoices
  - name: payments
    description: Payments & receipts
  - name: reports
    description: Business reports
  - name: dashboard
    description: Dashboard summary
  - name: installments
    description: EMI & installment plans
  - name: inventory
    description: Inventory management & stock movements
  - name: suppliers
    description: Suppliers & purchase invoices
  - name: cashbook
    description: Cashbook & expenses
  - name: lookups
    description: Configurable lookup values (categories & units)
  - name: auditLogs
    description: Audit trail of data-changing actions

paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  # ── AUTH ──────────────────────────────────────────────────────────────────
  /auth/login:
    post:
      operationId: login
      tags: [auth]
      summary: Login with username and password
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginInput"
      responses:
        "200":
          description: Logged in
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthUser"
        "401":
          description: Invalid credentials

  /auth/logout:
    post:
      operationId: logout
      tags: [auth]
      summary: Logout current session
      responses:
        "200":
          description: Logged out

  /auth/me:
    get:
      operationId: getMe
      tags: [auth]
      summary: Get current authenticated user
      responses:
        "200":
          description: Current user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthUser"
        "401":
          description: Not authenticated

  /users:
    get:
      operationId: listUsers
      tags: [auth]
      summary: List all users (owner only)
      responses:
        "200":
          description: Users list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/User"
    post:
      operationId: createUser
      tags: [auth]
      summary: Create a new user (owner only)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UserInput"
      responses:
        "201":
          description: User created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"

  /users/{id}:
    patch:
      operationId: updateUser
      tags: [auth]
      summary: Update a user
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UserUpdate"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
    delete:
      operationId: deleteUser
      tags: [auth]
      summary: Delete a user
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted

  # ── CUSTOMERS ─────────────────────────────────────────────────────────────
  /customers:
    get:
      operationId: listCustomers
      tags: [customers]
      summary: List all customers
      parameters:
        - name: search
          in: query
          schema:
            type: string
        - name: area
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Customer list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Customer"
    post:
      operationId: createCustomer
      tags: [customers]
      summary: Create a new customer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CustomerInput"
      responses:
        "201":
          description: Customer created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Customer"

  /customers/{id}:
    get:
      operationId: getCustomer
      tags: [customers]
      summary: Get a customer by ID
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Customer
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Customer"
    patch:
      operationId: updateCustomer
      tags: [customers]
      summary: Update a customer
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CustomerUpdate"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Customer"
    delete:
      operationId: deleteCustomer
      tags: [customers]
      summary: Delete a customer
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted

  /customers/{id}/ledger:
    get:
      operationId: getCustomerLedger
      tags: [customers]
      summary: Get a customer's detailed ledger with running balance
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: from
          in: query
          schema:
            type: string
            format: date
        - name: to
          in: query
          schema:
            type: string
            format: date
      responses:
        "200":
          description: Ledger entries
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerLedger"

  /customers/{id}/statement:
    get:
      operationId: getCustomerStatement
      tags: [customers]
      summary: Get WhatsApp-ready plain text statement
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Plain text statement
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerStatement"

  # ── PRODUCTS ──────────────────────────────────────────────────────────────
  /products:
    get:
      operationId: listProducts
      tags: [products]
      summary: List all products/cement brands
      responses:
        "200":
          description: Product list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Product"
    post:
      operationId: createProduct
      tags: [products]
      summary: Create a new product
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProductInput"
      responses:
        "201":
          description: Product created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Product"

  /products/{id}:
    get:
      operationId: getProduct
      tags: [products]
      summary: Get a product by ID
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Product
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Product"
    patch:
      operationId: updateProduct
      tags: [products]
      summary: Update a product (also logs new rate to rate history)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProductUpdate"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Product"
    delete:
      operationId: deleteProduct
      tags: [products]
      summary: Delete a product
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted

  /products/{id}/rates:
    get:
      operationId: getProductRates
      tags: [products]
      summary: Get rate history for a product
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Rate history
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ProductRate"

  # ── SALE ORDERS ───────────────────────────────────────────────────────────
  /sale-orders:
    get:
      operationId: listSaleOrders
      tags: [saleOrders]
      summary: List all sale orders
      parameters:
        - name: customerId
          in: query
          schema:
            type: integer
        - name: from
          in: query
          schema:
            type: string
            format: date
        - name: to
          in: query
          schema:
            type: string
            format: date
        - name: includeReversed
          in: query
          description: Include reversed originals and reversal rows (default excludes them — see the correction workflow).
          schema:
            type: boolean
      responses:
        "200":
          description: Sale order list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SaleOrder"
    post:
      operationId: createSaleOrder
      tags: [saleOrders]
      summary: Create a new sale order
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SaleOrderInput"
      responses:
        "201":
          description: Sale order created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SaleOrder"

  /sale-orders/{id}:
    get:
      operationId: getSaleOrder
      tags: [saleOrders]
      summary: Get a sale order with items
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Sale order
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SaleOrder"

  /sale-orders/{id}/correct:
    post:
      operationId: correctSaleOrder
      tags: [saleOrders]
      summary: Correct (or void) a posted sale order — never edits or deletes it in place
      description: >
        Standard correction workflow: the original row is never mutated (only its status
        flips to 'reversed') or deleted. This posts a 'reversal' row that cancels it, and
        — unless `void` is set — a new 'posted' row with the corrected data. Fails with
        409 if the target row isn't currently 'posted' (already reversed/corrected).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SaleOrderCorrectionInput"
      responses:
        "200":
          description: Correction applied
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SaleOrderCorrectionResult"
        "409":
          description: The target row is not currently posted

  # ── PAYMENTS ──────────────────────────────────────────────────────────────
  /payments:
    get:
      operationId: listPayments
      tags: [payments]
      summary: List all payments
      parameters:
        - name: customerId
          in: query
          schema:
            type: integer
        - name: from
          in: query
          schema:
            type: string
            format: date
        - name: to
          in: query
          schema:
            type: string
            format: date
        - name: type
          in: query
          schema:
            type: string
            enum: [cash, bank]
        - name: includeReversed
          in: query
          description: Include reversed originals and reversal rows (default excludes them — see the correction workflow).
          schema:
            type: boolean
      responses:
        "200":
          description: Payment list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Payment"
    post:
      operationId: createPayment
      tags: [payments]
      summary: Record a new payment
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PaymentInput"
      responses:
        "201":
          description: Payment recorded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Payment"

  /payments/{id}:
    get:
      operationId: getPayment
      tags: [payments]
      summary: Get a payment by ID
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Payment
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Payment"

  /payments/{id}/correct:
    post:
      operationId: correctPayment
      tags: [payments]
      summary: Correct (or void) a posted payment — never edits or deletes it in place
      description: >
        Same correction workflow as sale orders (see there for details). Also reverses
        and reposts the payment's auto-posted cashbook entry, so cashbook balance reflects
        the correction too.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PaymentCorrectionInput"
      responses:
        "200":
          description: Correction applied
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaymentCorrectionResult"
        "409":
          description: The target row is not currently posted

  # ── REPORTS ───────────────────────────────────────────────────────────────
  /reports/aging:
    get:
      operationId: getAgingReport
      tags: [reports]
      summary: Aging report — who owes how much for how long
      responses:
        "200":
          description: Aging data per customer
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AgingRow"

  /reports/daily-collection:
    get:
      operationId: getDailyCollectionReport
      tags: [reports]
      summary: Daily collection summary
      parameters:
        - name: date
          in: query
          schema:
            type: string
            format: date
      responses:
        "200":
          description: Daily collection
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DailyCollectionReport"

  /reports/monthly-sales:
    get:
      operationId: getMonthlySalesReport
      tags: [reports]
      summary: Monthly sales by product
      parameters:
        - name: year
          in: query
          schema:
            type: integer
        - name: month
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: Monthly sales
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MonthlySalesReport"

  /reports/daily-profit:
    get:
      operationId: getDailyProfitReport
      tags: [reports]
      summary: Daily profit report — revenue, COGS, gross profit, expenses, net profit
      parameters:
        - name: from
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: to
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: category
          in: query
          required: false
          schema:
            type: string
          description: Filter results to a specific product category
      responses:
        "200":
          description: Daily profit report
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DailyProfitReport"

  /reports/outstanding:
    get:
      operationId: getOutstandingReport
      tags: [reports]
      summary: Customer-wise outstanding list
      responses:
        "200":
          description: Outstanding list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/OutstandingRow"

  # ── DASHBOARD ─────────────────────────────────────────────────────────────
  /dashboard/summary:
    get:
      operationId: getDashboardSummary
      tags: [dashboard]
      summary: Dashboard at-a-glance numbers
      responses:
        "200":
          description: Dashboard summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardSummary"

  /dashboard/profit-breakdown:
    get:
      operationId: getProfitBreakdown
      tags: [dashboard]
      summary: Customer-wise, item-level profit breakdown for a single day (defaults to today)
      parameters:
        - name: date
          in: query
          required: false
          schema:
            type: string
            format: date
      responses:
        "200":
          description: Profit breakdown
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProfitBreakdown"

  /dashboard/top-debtors:
    get:
      operationId: getTopDebtors
      tags: [dashboard]
      summary: Top debtors list
      responses:
        "200":
          description: Top debtors
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DebtorRow"

  /dashboard/recent-activity:
    get:
      operationId: getRecentActivity
      tags: [dashboard]
      summary: Recent transactions (sales + payments)
      responses:
        "200":
          description: Recent activity feed
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ActivityItem"

  # ── INSTALLMENTS ──────────────────────────────────────────────────────────
  /installments:
    get:
      operationId: listInstallments
      tags: [installments]
      summary: List all installment plans
      responses:
        "200":
          description: Plans list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/InstallmentPlanDetail"
    post:
      operationId: createInstallmentPlan
      tags: [installments]
      summary: Create a new installment plan
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateInstallmentPlanInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InstallmentPlanDetail"

  /installments/{id}:
    get:
      operationId: getInstallmentPlan
      tags: [installments]
      summary: Get plan detail with schedule and payments
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Plan detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InstallmentPlanDetail"
    delete:
      operationId: deleteInstallmentPlan
      tags: [installments]
      summary: Delete an installment plan
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted

  /installments/{id}/payments:
    post:
      operationId: recordInstallmentPayment
      tags: [installments]
      summary: Record a payment against a plan
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InstallmentPaymentInput"
      responses:
        "201":
          description: Updated plan detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InstallmentPlanDetail"

  /installments/payments/{paymentId}/correct:
    post:
      operationId: correctInstallmentPayment
      tags: [installments]
      summary: Correct (or void) a posted installment payment — never edits or deletes it in place
      description: >
        Standard correction workflow (see /sale-orders/{id}/correct for the full
        explanation). No cashbook cascade — installment payments never auto-post to
        cashbook. Returns the updated plan detail directly.
      parameters:
        - name: paymentId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InstallmentPaymentCorrectionInput"
      responses:
        "200":
          description: Updated plan
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InstallmentPlanDetail"
        "409":
          description: The target row is not currently posted

  # ── INVENTORY ─────────────────────────────────────────────────────────────
  /inventory:
    get:
      operationId: listInventory
      tags: [inventory]
      summary: List all products with current stock levels
      responses:
        "200":
          description: Inventory list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/InventoryRow"

  /inventory/{productId}/movements:
    get:
      operationId: getInventoryMovements
      tags: [inventory]
      summary: Full stock movement ledger for a product
      parameters:
        - name: productId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Movement ledger
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InventoryMovements"

  /inventory/{productId}/settings:
    patch:
      operationId: updateInventorySettings
      tags: [inventory]
      summary: Update opening stock and minimum stock for a product
      parameters:
        - name: productId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InventorySettingsInput"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UpdateInventorySettingsResponse"

  /inventory/adjustments:
    post:
      operationId: createStockAdjustment
      tags: [inventory]
      summary: Create a manual stock adjustment
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StockAdjustmentInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StockAdjustment"

  /inventory/adjustments/{id}:
    delete:
      operationId: deleteStockAdjustment
      tags: [inventory]
      summary: Delete a stock adjustment
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted

  # ── SUPPLIERS ─────────────────────────────────────────────────────────────
  /suppliers:
    get:
      operationId: listSuppliers
      tags: [suppliers]
      summary: List all suppliers with payable balance
      responses:
        "200":
          description: Supplier list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SupplierRow"
    post:
      operationId: createSupplier
      tags: [suppliers]
      summary: Create a supplier
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SupplierInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupplierRow"

  /suppliers/{id}:
    get:
      operationId: getSupplier
      tags: [suppliers]
      summary: Get supplier with ledger (purchases + balance)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Supplier detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupplierDetail"
    patch:
      operationId: updateSupplier
      tags: [suppliers]
      summary: Update a supplier
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SupplierInput"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupplierRow"
    delete:
      operationId: deleteSupplier
      tags: [suppliers]
      summary: Delete a supplier
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted

  # ── PURCHASES ─────────────────────────────────────────────────────────────
  /purchases:
    get:
      operationId: listPurchases
      tags: [suppliers]
      summary: List purchase invoices
      parameters:
        - name: supplierId
          in: query
          schema:
            type: integer
        - name: from
          in: query
          schema:
            type: string
            format: date
        - name: to
          in: query
          schema:
            type: string
            format: date
        - name: includeReversed
          in: query
          description: Include reversed originals and reversal rows (default excludes them — see the correction workflow).
          schema:
            type: boolean
      responses:
        "200":
          description: Purchase list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PurchaseInvoiceRow"
    post:
      operationId: createPurchase
      tags: [suppliers]
      summary: Create a purchase invoice
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PurchaseInvoiceInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PurchaseInvoiceRow"

  /purchases/{id}:
    get:
      operationId: getPurchase
      tags: [suppliers]
      summary: Get purchase invoice with items
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Purchase detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PurchaseInvoiceDetail"

  /purchases/{id}/correct:
    post:
      operationId: correctPurchase
      tags: [suppliers]
      summary: Correct (or void) a posted purchase invoice — never edits or deletes it in place
      description: >
        Standard correction workflow (see /sale-orders/{id}/correct for the full
        explanation). Also reverses and reposts the invoice's auto-posted cashbook entry
        (if any) and re-applies updateCostPrice, so cashbook balance and product cost
        prices reflect the correction too.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PurchaseCorrectionInput"
      responses:
        "200":
          description: Correction applied
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PurchaseCorrectionResult"
        "409":
          description: The target row is not currently posted

  # ── CASHBOOK ──────────────────────────────────────────────────────────────
  /cashbook:
    get:
      operationId: listCashbookEntries
      tags: [cashbook]
      summary: List cashbook entries with running balance
      parameters:
        - name: from
          in: query
          schema:
            type: string
            format: date
        - name: to
          in: query
          schema:
            type: string
            format: date
        - name: type
          in: query
          schema:
            type: string
            enum: [cash_in, cash_out]
        - name: paymentMode
          in: query
          schema:
            type: string
            enum: [cash, bank, easypaisa, jazzcash, cheque, other]
        - name: includeReversed
          in: query
          description: Include reversed originals and reversal rows (default excludes them — see the correction workflow).
          schema:
            type: boolean
      responses:
        "200":
          description: Cashbook entries with running balance
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CashbookLedger"
    post:
      operationId: createCashbookEntry
      tags: [cashbook]
      summary: Create a manual cashbook entry
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CashbookEntryInput"
      responses:
        "201":
          description: Entry created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CashbookEntry"

  /cashbook/summary:
    get:
      operationId: getCashbookSummary
      tags: [cashbook]
      summary: Get cash & bank balances summary
      responses:
        "200":
          description: Balance summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CashbookSummary"

  /cashbook/{id}/correct:
    post:
      operationId: correctCashbookEntry
      tags: [cashbook]
      summary: Correct (or void) a posted cashbook entry — never edits or deletes it in place
      description: >
        Standard correction workflow (see /sale-orders/{id}/correct for the full
        explanation). Restricted to user-editable sources (manual, opening_balance,
        adjustment, salary, transfer) — auto-generated entries (payment/expense/purchase)
        must be corrected via their source transaction instead.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CashbookCorrectionInput"
      responses:
        "200":
          description: Correction applied
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CashbookCorrectionResult"
        "409":
          description: The target row is not currently posted

  # ── EXPENSES ──────────────────────────────────────────────────────────────
  /expenses:
    get:
      operationId: listExpenses
      tags: [cashbook]
      summary: List expenses
      parameters:
        - name: from
          in: query
          schema:
            type: string
            format: date
        - name: to
          in: query
          schema:
            type: string
            format: date
        - name: category
          in: query
          schema:
            type: string
        - name: includeReversed
          in: query
          description: Include reversed originals and reversal rows (default excludes them — see the correction workflow).
          schema:
            type: boolean
      responses:
        "200":
          description: Expense list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Expense"
    post:
      operationId: createExpense
      tags: [cashbook]
      summary: Record an expense (auto-posts to cashbook)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ExpenseInput"
      responses:
        "201":
          description: Expense recorded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Expense"

  /expenses/{id}/correct:
    post:
      operationId: correctExpense
      tags: [cashbook]
      summary: Correct (or void) a posted expense — never edits or deletes it in place
      description: >
        Standard correction workflow (see /sale-orders/{id}/correct for the full
        explanation). Also reverses and reposts the expense's auto-posted cashbook entry.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ExpenseCorrectionInput"
      responses:
        "200":
          description: Correction applied
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ExpenseCorrectionResult"
        "409":
          description: The target row is not currently posted

  # ── LOOKUPS ───────────────────────────────────────────────────────────────
  /lookups/{type}:
    get:
      operationId: listLookups
      tags: [lookups]
      summary: List all lookup values for a given type (category or unit)
      parameters:
        - name: type
          in: path
          required: true
          schema:
            type: string
            enum: [category, unit]
      responses:
        "200":
          description: Lookup values
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LookupValue"
    post:
      operationId: createLookup
      tags: [lookups]
      summary: Create a new lookup value (owner only)
      parameters:
        - name: type
          in: path
          required: true
          schema:
            type: string
            enum: [category, unit]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LookupInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LookupValue"
        "409":
          description: Value already exists

  /lookups/{type}/{id}:
    patch:
      operationId: updateLookup
      tags: [lookups]
      summary: Rename a lookup value (owner only)
      parameters:
        - name: type
          in: path
          required: true
          schema:
            type: string
            enum: [category, unit]
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LookupInput"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LookupValue"
    delete:
      operationId: deleteLookup
      tags: [lookups]
      summary: Delete a lookup value (owner only, blocked if in use)
      parameters:
        - name: type
          in: path
          required: true
          schema:
            type: string
            enum: [category, unit]
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted
        "409":
          description: Value is still in use by products

  /audit-logs:
    get:
      operationId: listAuditLogs
      tags: [auditLogs]
      summary: List audit log entries (owner only)
      parameters:
        - name: from
          in: query
          schema:
            type: string
            format: date
        - name: to
          in: query
          schema:
            type: string
            format: date
        - name: userId
          in: query
          schema:
            type: integer
        - name: action
          in: query
          schema:
            type: string
            enum: [create, update, delete]
        - name: entityType
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Audit log entries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AuditLog"

components:
  schemas:
    HealthStatus:
      type: object
      required: [status]
      properties:
        status:
          type: string

    # ── LOOKUPS ───────────────────────────────────────────────────────────────
    LookupValue:
      type: object
      required: [id, type, value, createdAt]
      properties:
        id:
          type: integer
        type:
          type: string
          enum: [category, unit]
        value:
          type: string
        createdAt:
          type: string
          format: date-time

    LookupInput:
      type: object
      required: [value]
      properties:
        value:
          type: string
          minLength: 1
          maxLength: 100

    # ── AUTH ──────────────────────────────────────────────────────────────
    LoginInput:
      type: object
      required: [username, password]
      properties:
        username:
          type: string
        password:
          type: string

    AuthUser:
      type: object
      required: [id, name, username, role]
      properties:
        id:
          type: integer
        name:
          type: string
        username:
          type: string
        role:
          type: string
          enum: [owner, salesman, cashier]

    User:
      type: object
      required: [id, name, username, role, createdAt]
      properties:
        id:
          type: integer
        name:
          type: string
        username:
          type: string
        role:
          type: string
          enum: [owner, salesman, cashier]
        createdAt:
          type: string
          format: date-time

    UserInput:
      type: object
      required: [name, username, password, role]
      properties:
        name:
          type: string
        username:
          type: string
        password:
          type: string
        role:
          type: string
          enum: [owner, salesman, cashier]

    UserUpdate:
      type: object
      properties:
        name:
          type: string
        password:
          type: string
        role:
          type: string
          enum: [owner, salesman, cashier]

    # ── CUSTOMERS ──────────────────────────────────────────────────────────
    Customer:
      type: object
      required: [id, name, openingBalance, balance, createdAt]
      properties:
        id:
          type: integer
        name:
          type: string
        ntn:
          type: ["string", "null"]
        area:
          type: ["string", "null"]
        address:
          type: ["string", "null"]
        contact:
          type: ["string", "null"]
        creditLimit:
          type: ["number", "null"]
        openingBalance:
          type: number
        openingBalanceDate:
          type: ["string", "null"]
          format: date
        balance:
          type: number
        createdAt:
          type: string
          format: date-time

    CustomerInput:
      type: object
      required: [name]
      properties:
        name:
          type: string
        ntn:
          type: string
        area:
          type: string
        address:
          type: string
        contact:
          type: string
        creditLimit:
          type: number
        openingBalance:
          type: number
        openingBalanceDate:
          type: string
          format: date

    CustomerUpdate:
      type: object
      properties:
        name:
          type: string
        ntn:
          type: ["string", "null"]
        area:
          type: string
        address:
          type: ["string", "null"]
        contact:
          type: string
        creditLimit:
          type: ["number", "null"]
        openingBalance:
          type: number
        openingBalanceDate:
          type: ["string", "null"]
          format: date

    LedgerEntry:
      type: object
      required: [srNo, date, transactionType, documentNo, receivedAmount, paidAmount, soValue, balance]
      properties:
        srNo:
          type: integer
        date:
          type: string
          format: date
        transactionType:
          type: string
        remarks:
          type: ["string", "null"]
        documentNo:
          type: ["string", "null"]
        billNo:
          type: ["string", "null"]
        item:
          type: ["string", "null"]
        unit:
          type: ["string", "null"]
        billtyNo:
          type: ["string", "null"]
        vehicleNo:
          type: ["string", "null"]
        qtyBags:
          type: ["number", "null"]
        rateBag:
          type: ["number", "null"]
        receivedAmount:
          type: number
        paidAmount:
          type: number
        soValue:
          type: number
        balance:
          type: number

    CategoryBreakdownItem:
      type: object
      required: [category, qty, amount, share]
      properties:
        category:
          type: string
        unit:
          type: ["string", "null"]
        qty:
          type: number
        amount:
          type: number
        share:
          type: number

    CustomerLedger:
      type: object
      required: [customer, openingBalance, openingBalanceDate, closingBalance, totalReceived, totalSoValue, from, to, entries, categoryBreakdown]
      properties:
        customer:
          $ref: "#/components/schemas/Customer"
        openingBalance:
          type: number
        openingBalanceDate:
          type: ["string", "null"]
          format: date
        closingBalance:
          type: number
        totalReceived:
          type: number
        totalPaid:
          type: number
        totalSoValue:
          type: number
        totalTons:
          type: number
        from:
          type: ["string", "null"]
          format: date
        to:
          type: ["string", "null"]
          format: date
        entries:
          type: array
          items:
            $ref: "#/components/schemas/LedgerEntry"
        categoryBreakdown:
          type: array
          items:
            $ref: "#/components/schemas/CategoryBreakdownItem"

    CustomerStatement:
      type: object
      required: [customerId, customerName, text]
      properties:
        customerId:
          type: integer
        customerName:
          type: string
        text:
          type: string

    # ── PRODUCTS ──────────────────────────────────────────────────────────
    Product:
      type: object
      required: [id, name, currentRate, unit, createdAt]
      properties:
        id:
          type: integer
        name:
          type: string
        costPrice:
          type: ["number", "null"]
        currentRate:
          type: number
        unit:
          type: string
        category:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time

    ProductInput:
      type: object
      required: [name, currentRate]
      properties:
        name:
          type: string
        currentRate:
          type: number
        costPrice:
          type: number

    ProductUpdate:
      type: object
      properties:
        name:
          type: string
        currentRate:
          type: number
        costPrice:
          type: ["number", "null"]

    ProductRate:
      type: object
      required: [id, productId, rate, effectiveDate]
      properties:
        id:
          type: integer
        productId:
          type: integer
        rate:
          type: number
        effectiveDate:
          type: string
          format: date

    # ── SALE ORDERS ────────────────────────────────────────────────────────
    SaleOrderItem:
      type: object
      required: [id, productId, productName, qty, rate, amount]
      properties:
        id:
          type: integer
        productId:
          type: integer
        productName:
          type: string
        qty:
          type: number
        rate:
          type: number
        amount:
          type: number
        notes:
          type: ["string", "null"]
        costPrice:
          type: ["number", "null"]

    SaleOrder:
      type: object
      required: [id, customerId, customerName, date, totalAmount, createdAt, items, status]
      properties:
        id:
          type: integer
        customerId:
          type: integer
        customerName:
          type: string
        date:
          type: string
          format: date
        vehicleNo:
          type: ["string", "null"]
        driverName:
          type: ["string", "null"]
        billtyNo:
          type: ["string", "null"]
        totalAmount:
          type: number
        notes:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time
        items:
          type: array
          items:
            $ref: "#/components/schemas/SaleOrderItem"
        status:
          type: string
          enum: [posted, reversed, reversal]
        reversesId:
          type: ["integer", "null"]
        correctsId:
          type: ["integer", "null"]

    SaleOrderItemInput:
      type: object
      required: [productId, qty]
      properties:
        productId:
          type: integer
        qty:
          type: number
        rate:
          type: number
        notes:
          type: string

    SaleOrderInput:
      type: object
      required: [customerId, date, items]
      properties:
        customerId:
          type: integer
        date:
          type: string
          format: date
        vehicleNo:
          type: string
        driverName:
          type: string
        billtyNo:
          type: string
        notes:
          type: string
        items:
          type: array
          items:
            $ref: "#/components/schemas/SaleOrderItemInput"

    SaleOrderCorrectionInput:
      type: object
      description: >
        Either set void=true to reverse the original with no replacement, or omit it and
        supply the corrected data (same shape as SaleOrderInput) to reverse-and-replace.
      properties:
        void:
          type: boolean
        reason:
          type: string
        customerId:
          type: integer
        date:
          type: string
          format: date
        vehicleNo:
          type: string
        driverName:
          type: string
        billtyNo:
          type: string
        notes:
          type: string
        items:
          type: array
          items:
            $ref: "#/components/schemas/SaleOrderItemInput"

    SaleOrderCorrectionResult:
      type: object
      required: [original, reversal]
      properties:
        original:
          $ref: "#/components/schemas/SaleOrder"
        reversal:
          $ref: "#/components/schemas/SaleOrder"
        correction:
          $ref: "#/components/schemas/SaleOrder"

    # ── PAYMENTS ──────────────────────────────────────────────────────────
    Payment:
      type: object
      required: [id, customerId, customerName, date, type, amount, createdAt, status]
      properties:
        id:
          type: integer
        customerId:
          type: integer
        customerName:
          type: string
        date:
          type: string
          format: date
        type:
          type: string
          enum: [cash, bank]
        amount:
          type: number
        bankAccount:
          type: ["string", "null"]
        chequeNo:
          type: ["string", "null"]
        notes:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time
        status:
          type: string
          enum: [posted, reversed, reversal]
        reversesId:
          type: ["integer", "null"]
        correctsId:
          type: ["integer", "null"]

    PaymentInput:
      type: object
      required: [customerId, date, type, amount]
      properties:
        customerId:
          type: integer
        date:
          type: string
          format: date
        type:
          type: string
          enum: [cash, bank]
        amount:
          type: number
        bankAccount:
          type: string
        chequeNo:
          type: string
        notes:
          type: string

    PaymentCorrectionInput:
      type: object
      description: >
        Either set void=true to reverse the original with no replacement, or omit it and
        supply the corrected data (same shape as PaymentInput) to reverse-and-replace.
      properties:
        void:
          type: boolean
        reason:
          type: string
        customerId:
          type: integer
        date:
          type: string
          format: date
        type:
          type: string
          enum: [cash, bank]
        amount:
          type: number
        bankAccount:
          type: string
        chequeNo:
          type: string
        notes:
          type: string

    PaymentCorrectionResult:
      type: object
      required: [original, reversal]
      properties:
        original:
          $ref: "#/components/schemas/Payment"
        reversal:
          $ref: "#/components/schemas/Payment"
        correction:
          $ref: "#/components/schemas/Payment"

    # ── REPORTS ───────────────────────────────────────────────────────────
    AgingRow:
      type: object
      required: [customerId, customerName, balance, days0to30, days31to60, days61to90, daysOver90]
      properties:
        customerId:
          type: integer
        customerName:
          type: string
        area:
          type: ["string", "null"]
        contact:
          type: ["string", "null"]
        balance:
          type: number
        days0to30:
          type: number
        days31to60:
          type: number
        days61to90:
          type: number
        daysOver90:
          type: number

    DailyCollectionReport:
      type: object
      required: [date, totalCash, totalBank, total, payments]
      properties:
        date:
          type: string
          format: date
        totalCash:
          type: number
        totalBank:
          type: number
        total:
          type: number
        payments:
          type: array
          items:
            $ref: "#/components/schemas/Payment"

    MonthlySalesProductRow:
      type: object
      required: [productId, productName, totalQty, totalAmount]
      properties:
        productId:
          type: integer
        productName:
          type: string
        totalQty:
          type: number
        totalAmount:
          type: number

    MonthlySalesReport:
      type: object
      required: [year, month, totalAmount, totalQty, byProduct]
      properties:
        year:
          type: integer
        month:
          type: integer
        totalAmount:
          type: number
        totalQty:
          type: number
        byProduct:
          type: array
          items:
            $ref: "#/components/schemas/MonthlySalesProductRow"

    OutstandingRow:
      type: object
      required: [customerId, customerName, balance, lastSaleDate, lastPaymentDate]
      properties:
        customerId:
          type: integer
        customerName:
          type: string
        area:
          type: ["string", "null"]
        contact:
          type: ["string", "null"]
        creditLimit:
          type: ["number", "null"]
        balance:
          type: number
        lastSaleDate:
          type: ["string", "null"]
        lastPaymentDate:
          type: ["string", "null"]
        isOverLimit:
          type: boolean

    # ── DASHBOARD ────────────────────────────────────────────────────────
    DashboardSummary:
      type: object
      required: [totalOutstanding, todayCollections, todaySales, todayProfit, totalCustomers, activeProducts]
      properties:
        totalOutstanding:
          type: number
        todayCollections:
          type: number
        todaySales:
          type: number
        todayProfit:
          type: number
        totalCustomers:
          type: integer
        activeProducts:
          type: integer

    ProfitBreakdownItem:
      type: object
      required: [productId, productName, category, unit, qty, amount, profit]
      properties:
        productId:
          type: integer
        productName:
          type: string
        category:
          type: ["string", "null"]
        unit:
          type: string
        qty:
          type: number
        amount:
          type: number
        profit:
          type: ["number", "null"]

    ProfitBreakdownCustomer:
      type: object
      required: [customerId, customerName, items, subtotalAmount, subtotalProfit]
      properties:
        customerId:
          type: integer
        customerName:
          type: string
        items:
          type: array
          items:
            $ref: "#/components/schemas/ProfitBreakdownItem"
        subtotalAmount:
          type: number
        subtotalProfit:
          type: number

    ProfitBreakdown:
      type: object
      required: [date, totalAmount, totalProfit, hasMissingCost, customers]
      properties:
        date:
          type: string
          format: date
        totalAmount:
          type: number
        totalProfit:
          type: number
        hasMissingCost:
          type: boolean
        customers:
          type: array
          items:
            $ref: "#/components/schemas/ProfitBreakdownCustomer"

    DebtorRow:
      type: object
      required: [customerId, customerName, balance]
      properties:
        customerId:
          type: integer
        customerName:
          type: string
        area:
          type: ["string", "null"]
        balance:
          type: number

    ActivityItem:
      type: object
      required: [id, type, date, customerName, amount, description]
      properties:
        id:
          type: integer
        type:
          type: string
          enum: [sale, payment]
        date:
          type: string
          format: date
        customerId:
          type: integer
        customerName:
          type: string
        amount:
          type: number
        description:
          type: string

    # ── INSTALLMENT SCHEMAS ───────────────────────────────────────────────
    InstallmentScheduleItem:
      type: object
      required: [id, planId, installmentNo, dueDate, scheduledAmount, paidAmount, balance, status]
      properties:
        id: { type: integer }
        planId: { type: integer }
        installmentNo: { type: integer }
        dueDate: { type: string }
        scheduledAmount: { type: number }
        paidAmount: { type: number }
        balance: { type: number }
        status:
          type: string
          enum: [pending, paid, overdue]

    InstallmentPaymentRecord:
      type: object
      required: [id, planId, date, amount, paymentMode, status]
      properties:
        id: { type: integer }
        planId: { type: integer }
        scheduleId: { type: ["integer", "null"] }
        date: { type: string }
        amount: { type: number }
        paymentMode: { type: string }
        notes: { type: ["string", "null"] }
        createdAt: {}
        status:
          type: string
          enum: [posted, reversed, reversal]
        reversesId: { type: ["integer", "null"] }
        correctsId: { type: ["integer", "null"] }

    InstallmentPaymentCorrectionInput:
      type: object
      description: >
        Either set void=true to reverse the original with no replacement, or omit it and
        supply the corrected data (same shape as InstallmentPaymentInput) to
        reverse-and-replace.
      properties:
        void:
          type: boolean
        reason:
          type: string
        scheduleId:
          type: ["integer", "null"]
        date:
          type: string
          format: date
        amount:
          type: number
        paymentMode:
          type: string
          enum: [cash, bank_transfer, cheque, online]
        notes:
          type: string

    InstallmentPlanDetail:
      type: object
      required: [id, customerId, customerName, title, totalAmount, downPayment, installmentsCount, frequency, startDate, totalPaid, outstanding, isFullyPaid, overdueCount, status, schedule, payments]
      properties:
        id: { type: integer }
        customerId: { type: integer }
        customerName: { type: string }
        saleOrderId: { type: ["integer", "null"] }
        saleOrderRef: { type: ["string", "null"] }
        title: { type: string }
        totalAmount: { type: number }
        downPayment: { type: number }
        installmentsCount: { type: integer }
        frequency: { type: string }
        startDate: { type: string }
        notes: { type: ["string", "null"] }
        createdAt: {}
        totalPaid: { type: number }
        outstanding: { type: number }
        isFullyPaid: { type: boolean }
        overdueCount: { type: integer }
        nextDueDate: { type: ["string", "null"] }
        nextDueAmount: { type: ["number", "null"] }
        status:
          type: string
          enum: [active, paid, overdue]
        schedule:
          type: array
          items:
            $ref: "#/components/schemas/InstallmentScheduleItem"
        payments:
          type: array
          items:
            $ref: "#/components/schemas/InstallmentPaymentRecord"

    CreateInstallmentPlanInput:
      type: object
      required: [customerId, title, totalAmount, installmentsCount, startDate]
      properties:
        customerId: { type: integer }
        saleOrderId: { type: integer }
        title: { type: string }
        totalAmount: { type: number }
        downPayment: { type: number }
        installmentsCount: { type: integer }
        frequency:
          type: string
          enum: [weekly, biweekly, monthly]
        startDate: { type: string, format: date }
        notes: { type: string }

    InstallmentPaymentInput:
      type: object
      required: [date, amount]
      properties:
        scheduleId: { type: integer }
        date: { type: string, format: date }
        amount: { type: number }
        paymentMode:
          type: string
          enum: [cash, bank_transfer, cheque, online]
        notes: { type: string }

    # ── INVENTORY SCHEMAS ─────────────────────────────────────────────────
    InventoryRow:
      type: object
      required: [id, name, currentRate, openingStock, minStock, purchased, sold, adjusted, currentStock, status]
      properties:
        id:
          type: integer
        name:
          type: string
        currentRate:
          type: number
        costPrice:
          type: ["number", "null"]
        openingStock:
          type: number
        minStock:
          type: number
        purchased:
          type: number
        sold:
          type: number
        adjusted:
          type: number
        currentStock:
          type: number
        status:
          type: string
          enum: [ok, low, out]

    InventoryMovementEntry:
      type: object
      required: [id, type, date, qty, ref, balance]
      properties:
        id:
          type: string
        type:
          type: string
          enum: [opening, in, out, adj_in, adj_out]
        date:
          type: string
        qty:
          type: number
        ref:
          type: string
        notes:
          type: ["string", "null"]
        balance:
          type: number
        adjustmentId:
          type: integer

    InventoryMovements:
      type: object
      required: [product, movements]
      properties:
        product:
          $ref: "#/components/schemas/InventoryRow"
        movements:
          type: array
          items:
            $ref: "#/components/schemas/InventoryMovementEntry"

    InventorySettingsInput:
      type: object
      properties:
        openingStock:
          type: number
          minimum: 0
        minStock:
          type: number
          minimum: 0

    StockAdjustmentInput:
      type: object
      required: [productId, date, qty, reason]
      properties:
        productId:
          type: integer
        date:
          type: string
          format: date
        qty:
          type: number
        reason:
          type: string
        notes:
          type: string

    UpdateInventorySettingsResponse:
      type: object
      required: [id, openingStock, minStock]
      properties:
        id:
          type: integer
        openingStock:
          type: number
        minStock:
          type: number

    StockAdjustment:
      type: object
      required: [id, productId, date, qty, reason, createdAt]
      properties:
        id:
          type: integer
        productId:
          type: integer
        date:
          type: string
          format: date
        qty:
          type: number
        reason:
          type: string
        notes:
          type: ["string", "null"]
        createdById:
          type: ["integer", "null"]
        createdAt:
          type: string
          format: date-time

    # ── DAILY PROFIT SCHEMAS ──────────────────────────────────────────────
    DailyProfitDay:
      type: object
      required: [date, revenue, cogs, grossProfit, grossMargin, expenses, netProfit, netMargin, orders, qty]
      properties:
        date:
          type: string
          format: date
        revenue:
          type: number
        cogs:
          type: number
        grossProfit:
          type: number
        grossMargin:
          type: number
        expenses:
          type: number
        netProfit:
          type: number
        netMargin:
          type: number
        orders:
          type: integer
        qty:
          type: number

    DailyProfitProductRow:
      type: object
      required: [productId, productName, qty, revenue, cogs, profit, margin]
      properties:
        productId:
          type: integer
        productName:
          type: string
        qty:
          type: number
        revenue:
          type: number
        cogs:
          type: number
        profit:
          type: number
        margin:
          type: number

    DailyProfitSummary:
      type: object
      required: [revenue, cogs, grossProfit, grossMargin, expenses, netProfit, netMargin, orders, qty]
      properties:
        revenue:
          type: number
        cogs:
          type: number
        grossProfit:
          type: number
        grossMargin:
          type: number
        expenses:
          type: number
        netProfit:
          type: number
        netMargin:
          type: number
        orders:
          type: integer
        qty:
          type: number

    DailyProfitReport:
      type: object
      required: [from, to, summary, days, byProduct]
      properties:
        from:
          type: string
          format: date
        to:
          type: string
          format: date
        summary:
          $ref: "#/components/schemas/DailyProfitSummary"
        days:
          type: array
          items:
            $ref: "#/components/schemas/DailyProfitDay"
        byProduct:
          type: array
          items:
            $ref: "#/components/schemas/DailyProfitProductRow"

    # ── SUPPLIER / PURCHASE SCHEMAS ───────────────────────────────────────
    SupplierInput:
      type: object
      required: [name]
      properties:
        name:
          type: string
        contact:
          type: string
        address:
          type: string
        ntn:
          type: string
        openingBalance:
          type: number
          default: 0
        openingBalanceDate:
          type: string
          format: date

    SupplierRow:
      type: object
      required: [id, name, openingBalance, payableBalance, createdAt]
      properties:
        id:
          type: integer
        name:
          type: string
        contact:
          type: ["string", "null"]
        address:
          type: ["string", "null"]
        ntn:
          type: ["string", "null"]
        openingBalance:
          type: number
        payableBalance:
          type: number
        createdAt:
          type: string
          format: date-time

    SupplierDetail:
      type: object
      required: [id, name, openingBalance, payableBalance, createdAt, invoices]
      properties:
        id:
          type: integer
        name:
          type: string
        contact:
          type: ["string", "null"]
        address:
          type: ["string", "null"]
        ntn:
          type: ["string", "null"]
        openingBalance:
          type: number
        payableBalance:
          type: number
        createdAt:
          type: string
          format: date-time
        invoices:
          type: array
          items:
            $ref: "#/components/schemas/PurchaseInvoiceRow"

    PurchaseInvoiceItemInput:
      type: object
      required: [productId, qty, rate]
      properties:
        productId:
          type: integer
        qty:
          type: number
        rate:
          type: number

    PurchaseInvoiceInput:
      type: object
      required: [supplierId, date, items]
      properties:
        supplierId:
          type: integer
        date:
          type: string
          format: date
        invoiceNo:
          type: string
        items:
          type: array
          items:
            $ref: "#/components/schemas/PurchaseInvoiceItemInput"
        paidAmount:
          type: number
          default: 0
        paymentMode:
          type: string
          enum: [cash, bank, easypaisa, jazzcash, cheque, other]
          default: cash
        notes:
          type: string
        updateCostPrice:
          type: boolean
          default: true

    PurchaseInvoiceRow:
      type: object
      required: [id, supplierId, supplierName, date, totalAmount, paidAmount, balance, paymentMode, createdAt, status]
      properties:
        id:
          type: integer
        supplierId:
          type: integer
        supplierName:
          type: string
        date:
          type: string
          format: date
        invoiceNo:
          type: ["string", "null"]
        totalAmount:
          type: number
        paidAmount:
          type: number
        balance:
          type: number
        paymentMode:
          type: string
          enum: [cash, bank, easypaisa, jazzcash, cheque, other]
        notes:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time
        status:
          type: string
          enum: [posted, reversed, reversal]
        reversesId:
          type: ["integer", "null"]
        correctsId:
          type: ["integer", "null"]

    PurchaseInvoiceItemRow:
      type: object
      required: [id, productId, productName, qty, rate, amount]
      properties:
        id:
          type: integer
        productId:
          type: integer
        productName:
          type: string
        qty:
          type: number
        rate:
          type: number
        amount:
          type: number

    PurchaseInvoiceDetail:
      type: object
      required: [id, supplierId, supplierName, date, totalAmount, paidAmount, balance, paymentMode, createdAt, items, status]
      properties:
        id:
          type: integer
        supplierId:
          type: integer
        supplierName:
          type: string
        date:
          type: string
          format: date
        invoiceNo:
          type: ["string", "null"]
        totalAmount:
          type: number
        paidAmount:
          type: number
        balance:
          type: number
        paymentMode:
          type: string
          enum: [cash, bank, easypaisa, jazzcash, cheque, other]
        notes:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time
        items:
          type: array
          items:
            $ref: "#/components/schemas/PurchaseInvoiceItemRow"
        status:
          type: string
          enum: [posted, reversed, reversal]
        reversesId:
          type: ["integer", "null"]
        correctsId:
          type: ["integer", "null"]

    PurchaseCorrectionInput:
      type: object
      description: >
        Either set void=true to reverse the original with no replacement, or omit it and
        supply the corrected data (same shape as PurchaseInvoiceInput) to
        reverse-and-replace.
      properties:
        void:
          type: boolean
        reason:
          type: string
        supplierId:
          type: integer
        date:
          type: string
          format: date
        invoiceNo:
          type: string
        items:
          type: array
          items:
            $ref: "#/components/schemas/PurchaseInvoiceItemInput"
        paidAmount:
          type: number
        paymentMode:
          type: string
          enum: [cash, bank, easypaisa, jazzcash, cheque, other]
        notes:
          type: string
        updateCostPrice:
          type: boolean
          default: true

    PurchaseCorrectionResult:
      type: object
      required: [original, reversal]
      properties:
        original:
          $ref: "#/components/schemas/PurchaseInvoiceRow"
        reversal:
          $ref: "#/components/schemas/PurchaseInvoiceRow"
        correction:
          $ref: "#/components/schemas/PurchaseInvoiceRow"

    # ── CASHBOOK SCHEMAS ──────────────────────────────────────────────────
    CashbookEntry:
      type: object
      required: [id, date, type, source, description, paymentMode, amount, runningBalance, createdAt, status]
      properties:
        id:
          type: integer
        date:
          type: string
          format: date
        type:
          type: string
          enum: [cash_in, cash_out]
        source:
          type: string
          enum: [manual, payment, expense, purchase, opening_balance, adjustment, salary, transfer]
        referenceId:
          type: ["integer", "null"]
        description:
          type: string
        paymentMode:
          type: string
          enum: [cash, bank, easypaisa, jazzcash, cheque, other]
        amount:
          type: number
        runningBalance:
          type: number
        notes:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time
        status:
          type: string
          enum: [posted, reversed, reversal]
        reversesId:
          type: ["integer", "null"]
        correctsId:
          type: ["integer", "null"]

    CashbookEntryInput:
      type: object
      required: [date, type, description, paymentMode, amount]
      properties:
        date:
          type: string
          format: date
        type:
          type: string
          enum: [cash_in, cash_out]
        source:
          type: string
          enum: [manual, opening_balance, adjustment, salary, transfer]
          default: manual
        description:
          type: string
        paymentMode:
          type: string
          enum: [cash, bank, easypaisa, jazzcash, cheque, other]
        amount:
          type: number
          minimum: 0.01
        notes:
          type: string

    CashbookCorrectionInput:
      type: object
      description: >
        Either set void=true to reverse the original with no replacement, or omit it and
        supply the corrected data (same shape as CashbookEntryInput) to
        reverse-and-replace. Only valid for user-editable sources.
      properties:
        void:
          type: boolean
        reason:
          type: string
        date:
          type: string
          format: date
        type:
          type: string
          enum: [cash_in, cash_out]
        source:
          type: string
          enum: [manual, opening_balance, adjustment, salary, transfer]
        description:
          type: string
        paymentMode:
          type: string
          enum: [cash, bank, easypaisa, jazzcash, cheque, other]
        amount:
          type: number
          minimum: 0.01
        notes:
          type: string

    CashbookCorrectionResult:
      type: object
      required: [original, reversal]
      properties:
        original:
          $ref: "#/components/schemas/CashbookEntry"
        reversal:
          $ref: "#/components/schemas/CashbookEntry"
        correction:
          $ref: "#/components/schemas/CashbookEntry"

    CashbookLedger:
      type: object
      required: [entries, totalIn, totalOut, netBalance]
      properties:
        entries:
          type: array
          items:
            $ref: "#/components/schemas/CashbookEntry"
        totalIn:
          type: number
        totalOut:
          type: number
        netBalance:
          type: number

    CashbookSummary:
      type: object
      required: [cashInHand, bankBalance, easypaisaBalance, jazzcashBalance, totalBalance, todayIn, todayOut]
      properties:
        cashInHand:
          type: number
        bankBalance:
          type: number
        easypaisaBalance:
          type: number
        jazzcashBalance:
          type: number
        totalBalance:
          type: number
        todayIn:
          type: number
        todayOut:
          type: number

    Expense:
      type: object
      required: [id, date, category, description, amount, paymentMode, createdAt, status]
      properties:
        id:
          type: integer
        date:
          type: string
          format: date
        category:
          type: string
        description:
          type: string
        amount:
          type: number
        paymentMode:
          type: string
          enum: [cash, bank, easypaisa, jazzcash, cheque, other]
        notes:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time
        status:
          type: string
          enum: [posted, reversed, reversal]
        reversesId:
          type: ["integer", "null"]
        correctsId:
          type: ["integer", "null"]

    ExpenseCorrectionInput:
      type: object
      description: >
        Either set void=true to reverse the original with no replacement, or omit it and
        supply the corrected data (same shape as ExpenseInput) to reverse-and-replace.
      properties:
        void:
          type: boolean
        reason:
          type: string
        date:
          type: string
          format: date
        category:
          type: string
        description:
          type: string
        amount:
          type: number
        paymentMode:
          type: string
          enum: [cash, bank, easypaisa, jazzcash, cheque, other]
        notes:
          type: string

    ExpenseCorrectionResult:
      type: object
      required: [original, reversal]
      properties:
        original:
          $ref: "#/components/schemas/Expense"
        reversal:
          $ref: "#/components/schemas/Expense"
        correction:
          $ref: "#/components/schemas/Expense"

    ExpenseInput:
      type: object
      required: [date, category, description, amount, paymentMode]
      properties:
        date:
          type: string
          format: date
        category:
          type: string
        description:
          type: string
        amount:
          type: number
          minimum: 0.01
        paymentMode:
          type: string
          enum: [cash, bank, easypaisa, jazzcash, cheque, other]
        notes:
          type: string

    AuditLog:
      type: object
      required: [id, userId, userName, method, path, entityType, entityId, action, description, createdAt]
      properties:
        id:
          type: integer
        userId:
          type: ["integer", "null"]
        userName:
          type: ["string", "null"]
        method:
          type: string
        path:
          type: string
        entityType:
          type: string
        entityId:
          type: ["integer", "null"]
        action:
          type: string
          enum: [create, update, delete]
        description:
          type: string
        createdAt:
          type: string
          format: date-time
